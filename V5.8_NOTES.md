# v5.8 — Live verification progress

Server/dashboard-only release.

- Shows queued, checking, completed, and remaining counts while server verification runs.
- Shows per-ISBN status and elapsed time.
- Polls /admin automatically every 1.5 seconds while a verification run is active.
- Does not change client lookup or library behavior.
